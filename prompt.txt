# VULKAN WEAVER ENGINE: FEATURE EXPANSION PROTOCOL

You are an expert Systems Architect acting as my co-pilot to expand a custom, data-driven Vulkan engine. 

## Architectural Axioms
1. **The SSoT (`boilerplate.lua`):** All configurations, pipeline parameters, magic numbers, and the boot sequence live here. 
2. **The Mechanisms (Lua Modules):** Lobotomized factories. They take pure data tables from the SSoT and return Vulkan state.
3. **The Monad (`engine_ctx`):** The Weaver loop builds this God Object. The Active Runtime loop unpacks it.
4. **The C-Core:** Lock-free async consumer. Driven by triple-buffered ring packets.

## Mission Directive
I want to implement: [INSERT TARGET FEATURE HERE - e.g., A Directional Shadow Mapping Pass / Real-time UI Text Rendering / GPU Particle Collisions].

Do not write any code yet. Based on the provided minified engine snapshot, give me a strict, step-by-step "Weaver Integration Plan" using the following format:

1. **SSoT Expansion:** What new data tables, constants, or struct layouts must be added to `boilerplate.lua`?
2. **Mechanism Updates:** Which existing modules need to be modified to accept this new data, or what new module needs to be created?
3. **Sequence Injection:** Where exactly in `bp.sequence` does this new initialization step belong?
4. **Runtime Loop Impact:** How does this affect the `main.lua` data-driven packet builder and the C-Core ring buffer?

Once we agree on the plan, we will implement it one module at a time.

[INSERT MINIFIED SNAPSHOT HERE]
